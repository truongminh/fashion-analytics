
async function TrackingFactory({ DB_URL }) {
    const start = (group, metadata) => {
        const end = () => {
        }
        return { end };
    }
    return { start };
}


module.exports = {
    TrackingFactory
}
